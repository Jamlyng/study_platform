meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"
build:
  requirementsPath: requirements.txt
run:
  command: gunicorn "run:create_app()"
  persistence:
    - path: study_platform.db
  persistenceMount: /data
  containerPort: ""
